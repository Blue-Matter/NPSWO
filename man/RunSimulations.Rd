% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-oms.R
\name{RunSimulations}
\alias{RunSimulations}
\title{Simulate Operating Models}
\usage{
RunSimulations(OM_names, DoRefLandings = FALSE, parallel = FALSE, ...)
}
\arguments{
\item{OM_names}{\code{character} vector of Operating Model names to simulate.}

\item{DoRefLandings}{Logical. Passed to \code{\link[MSEtool:Simulate]{MSEtool::Simulate()}}. Default: \code{FALSE}.}

\item{parallel}{\verb{Logical.. Should simulations be run in parallel when }length(OM_names) > 1\code{? Default }FALSE`. Ignored for single OMs.}

\item{...}{Additional arguments passed to \code{\link[MSEtool:Simulate]{MSEtool::Simulate()}}.}
}
\value{
Invisibly returns a list of \code{Hist} objects, one per OM.
}
\description{
Loads and runs \code{\link[MSEtool:Simulate]{MSEtool::Simulate()}} for one or more Operating Models (OMs),
saving each result to disk. When multiple OMs are provided and \code{parallel = TRUE},
simulations are run in parallel using \code{future.apply}.
}
\details{
When \code{length(OM_names) > 1} and \code{parallel = TRUE}, \code{\link[MSEtool:SetupParallel]{MSEtool::SetupParallel()}}
is called with \code{workers = 2}. This can use a lot of memory with large OMs
so only run with \code{parallel=TRUE} if you have a lot of memory!

When \code{parallel = FALSE} or only a single OM is provided, simulations run
sequentially in the current session.
}
\seealso{
\code{\link[MSEtool:Simulate]{MSEtool::Simulate()}}, \code{\link[=LoadOM]{LoadOM()}}, \code{\link[=SaveHist]{SaveHist()}}, \code{\link[MSEtool:SetupParallel]{MSEtool::SetupParallel()}}
}
