% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-lh-samples.R
\name{Generate_LH_Samples}
\alias{Generate_LH_Samples}
\title{Sample Life-History Parameters Using FishLife Priors}
\usage{
Generate_LH_Samples(
  Parameters,
  Genus = "Xiphias",
  Species = "gladius",
  same_par_cor = 0.5,
  nSim = 200,
  truncSD = 1.96,
  seed = 101,
  plot = FALSE
)
}
\arguments{
\item{Parameters}{A \code{data.frame} with the following columns:
\itemize{
\item \code{Par} — parameter name; one of \code{"Linf"}, \code{"K"}, \code{"M"}, \code{"Lm"}, \code{"CR"}
\item \code{Mean} — mean value on the natural scale
\item \code{SD} — standard deviation on the log scale
\item \code{Stock} \emph{(optional)} — stock or sex label (e.g. \code{"Female"}, \code{"Male"}).
Rows without a \code{Stock} value (or \code{NA}) are treated as applying to all
stocks. Each unique \code{Par}–\code{Stock} combination becomes one column in the
output.
}}

\item{Genus}{\code{character}. Genus name passed to the FishLife database lookup
(default \code{"Xiphias"}).}

\item{Species}{\code{character}. Species name passed to the FishLife database
lookup (default \code{"gladius"}).}

\item{same_par_cor}{\code{numeric} in [-1, 1]. Correlation applied between the
same parameter across stocks (e.g. \code{M_Female} and \code{M_Male}). Default
\code{0.5}.}

\item{nSim}{\code{integer}. Number of samples to draw. Default \code{200}.}

\item{truncSD}{\code{numeric}. Truncation point in standard deviations applied
symmetrically around the log-scale mean. Default \code{1.96}.}

\item{seed}{\code{integer}. Random seed for reproducibility. Default \code{101}.}

\item{plot}{\code{logical}. If \code{TRUE}, a pairs plot of the sampled values is
displayed. Default \code{FALSE}.
\subsection{Correlation structure}{

The full parameter correlation matrix is constructed in two steps:
\enumerate{
\item \strong{Cross-parameter correlations} (e.g. \code{M} vs \code{h}) are taken directly
from the FishLife covariance matrix for the target species.
\item \strong{Within-parameter, cross-stock correlations} (e.g. \code{M_Female} vs
\code{M_Male}) are set to \code{same_par_cor}, which the user specifies to reflect
their prior belief about how tightly the same trait co-varies across
stocks or sexes.
}
}

\subsection{Log-scale sampling}{

All parameters are sampled on the log scale and back-transformed. Means and
standard deviations in \code{Parameters} should therefore be on the natural
scale for \code{Mean} and the log scale for \code{SD}.
}

\subsection{Attribution}{

This methodology is adapted from the Southern Swordfish MSE developed by
Nathan Taylor (ICCAT).
}}
}
\value{
A \code{data.frame} with \code{nSim} rows and one column per \code{Par}–\code{Stock}
combination. \code{CR} columns are returned as steepness (\code{h}).
}
\description{
Generates correlated samples of life-history parameters by combining
user-specified means and standard deviations with the correlation structure
from the \href{https://github.com/James-Thorson-NOAA/FishLife}{FishLife} database.
Parameters may be stock-specific (e.g. sex-specific natural mortality) or
shared across stocks. Sampling is performed on the log scale using a
truncated multivariate normal distribution.
}
\details{
The compensation ratio (\code{CR}) is accepted as an alias for steepness (\code{h})
and is converted internally via \code{\link[MSEtool:CR2h]{MSEtool::h2CR()}}; returned values are
back-converted to \code{h}.
}
\examples{
\dontrun{
# Sex-specific natural mortality
Par_M <- data.frame(
  Par   = "M",
  Mean  = c(0.22, 0.36),
  SD    = 0.1,
  Stock = c("Female", "Male")
)

# Compensation ratio (steepness), shared across stocks
Par_CR <- data.frame(
  Par  = "CR",
  Mean = MSEtool::h2CR(0.9),
  SD   = 0.25
)

Parameters <- dplyr::bind_rows(Par_M, Par_CR)

Samples <- Generate_LH_Samples(Parameters, nSim = 500, plot = TRUE)
}

}
