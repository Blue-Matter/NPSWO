% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition-ss3.R
\name{WriteSSFiles}
\alias{WriteSSFiles}
\title{Write SS3 Input Files for a Single Simulation}
\usage{
WriteSSFiles(
  i,
  StochasticValues,
  SS3OutDir,
  dat,
  ctl,
  starter,
  forecast,
  OffFleets = NULL
)
}
\arguments{
\item{i}{Integer. Row index into \code{StochasticValues};
also determines the subdirectory name (zero-padded).}

\item{StochasticValues}{Data frame. Sampled parameter sets with columns
\code{M_female}, \code{M_male}, and \code{h}.}

\item{SS3OutDir}{Character. Parent directory in which the numbered
simulation subdirectory will be created.}

\item{dat}{List. SS3 data object from \code{r4ss::SS_readdat_3.30()}.}

\item{ctl}{List. SS3 control object from \code{r4ss::SS_readctl_3.30()}.}

\item{starter}{List. SS3 starter object from \code{r4ss::SS_readstarter()}.}

\item{forecast}{List. SS3 forecast object from \code{r4ss::SS_readforecast()}.}

\item{OffFleets}{Integer vector or \code{NULL}. Fleet indices to
deactivate by negating their year values in catch,
length composition, and CPUE data. Default: \code{NULL}.}
}
\value{
Invisibly returns the path to the simulation subdirectory.
}
\description{
Applies stochastic life-history parameters (natural mortality and steepness)
for simulation \code{i} to a set of SS3 model objects, creates a numbered
subdirectory, and writes all four SS3 input files to disk.
}
\details{
Natural mortality vectors are rescaled relative to their terminal age value,
preserving the age-varying shape while shifting the overall level to the
sampled value. Steepness is applied to both the \code{INIT} and \code{PRIOR}
fields of the \code{SR_BH_steep} parameter row.

The starter is configured to use control file initial values
(\code{init_values_src = 0}), suppress run display output, and disable the
cumulative report. A minimum of one forecast year is enforced.
}
\seealso{
\code{\link[=CreateSSDirectories]{CreateSSDirectories()}}
}
