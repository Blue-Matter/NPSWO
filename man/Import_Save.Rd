% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-save.R
\name{Import_Save}
\alias{Import_Save}
\title{Import SS3 Output and Save Operating Model}
\usage{
Import_Save(OM_Name, DropFleets = NULL)
}
\arguments{
\item{OM_Name}{\code{character(1)}. The name of the Operating Model. Used to
locate the SS3 report files via \code{\link[=ImportRepList]{ImportRepList()}} and as the identifier
passed to \code{\link[=SaveOM]{SaveOM()}}.}

\item{DropFleets}{\code{character} or \code{integer} vector of fleet names or indices
to remove from the OM. Fleet names are matched against
\code{\link[MSEtool:name-accessors]{MSEtool::FleetNames()}} and converted to indices before subsetting.
If \code{NULL} (default), all fleets are retained.}
}
\value{
Called for its side effects. Invisibly returns \code{NULL}. The
constructed \link[MSEtool:OM-legacy-class]{MSEtool::OM} object is saved to disk via \code{\link[=SaveOM]{SaveOM()}}.
}
\description{
Reads SS3 report files for a named Operating Model, combines them into an
\link[MSEtool:OM-legacy-class]{MSEtool::OM} object via \code{\link[MSEtool:ImportSS]{MSEtool::ImportSS()}}, optionally drops
specified fleets, applies control specifications, and saves the result to
disk. All specifications and global variables, including \code{ControlList}, are
sourced from \verb{0. Specifications.R}.
}
\details{
The function proceeds in five steps:
\enumerate{
\item \strong{Specifications} — \verb{0. Specifications.R} is sourced to populate
\code{Name}, \code{pYear}, \code{StockName}, \code{Species}, and \code{ControlList} in the
local environment.
\item \strong{Import} — \code{\link[=ImportRepList]{ImportRepList()}} reads SS3 report files from all
subdirectories associated with \code{OM_Name} into a list.
\item \strong{Conversion} — \code{\link[MSEtool:ImportSS]{MSEtool::ImportSS()}} combines the report list into
a single \link[MSEtool:OM-legacy-class]{MSEtool::OM} object. This step may take several minutes
depending on the number of SS3 models (i.e. the length of \code{RepList},
which corresponds to \code{nSim}).
\item \strong{Drop fleets} — If \code{DropFleets} is not \code{NULL}, fleet names or indices
are resolved via \code{\link[MSEtool:name-accessors]{MSEtool::FleetNames()}} and the remaining fleets are
retained using \code{\link[MSEtool:Subset]{MSEtool::Subset()}}.
\item \strong{Save} — \code{\link[MSEtool:OM-accessors]{MSEtool::Control()}} is assigned from \code{ControlList} and the
OM is written to the package data store via \code{\link[=SaveOM]{SaveOM()}} with
\code{overwrite = TRUE}.
}
}
\seealso{
\code{\link[=ImportRepList]{ImportRepList()}}, \code{\link[MSEtool:ImportSS]{MSEtool::ImportSS()}}, \code{\link[MSEtool:name-accessors]{MSEtool::FleetNames()}},
\code{\link[MSEtool:Subset]{MSEtool::Subset()}}, \code{\link[=SaveOM]{SaveOM()}}, \code{\link[=RunSimulations]{RunSimulations()}}
}
