% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-hist.R
\name{SaveLoadHist}
\alias{SaveLoadHist}
\alias{SaveHist}
\alias{ListHist}
\alias{LoadHist}
\title{Save, List, and Load Hist Objects}
\usage{
SaveHist(Hist, name, path = "Objects/Hist", compress = TRUE, overwrite = FALSE)

ListHist(path = "Objects/Hist", silent = FALSE)

LoadHist(name, path = "Objects/Hist")
}
\arguments{
\item{Hist}{A \link[MSEtool:hist-class]{MSEtool::hist} object.}

\item{name}{Character. Name of the object (without extension), used as the
file name for saving and loading.}

\item{path}{Character. Directory path where \code{.hist} files are saved and
read from. Created recursively by \code{SaveHist()} if it does not exist.
Default: \code{"Objects/Hist"}.}

\item{compress}{Logical. Whether to call \code{\link[=Compress]{Compress()}} on \code{Hist} before
saving, reducing object size by dropping redundant array dimensions.
Default: \code{TRUE}.}

\item{overwrite}{Logical. Whether to overwrite an existing file of the same
name. Default: \code{FALSE}.}

\item{silent}{Logical. Whether to suppress the printed list of available
objects. Default: \code{FALSE}.}
}
\value{
\itemize{
\item \code{SaveHist()}: Invisibly returns \code{NULL}. Called for its side-effect of
writing \verb{<path>/<name>.hist} to disk.
\item \code{ListHist()}: Invisibly returns a character vector of available file names
(without extension).
\item \code{LoadHist()}: Returns an expanded \link[MSEtool:hist-class]{MSEtool::hist} object with array
slots restored to full \verb{nSim x nYear} dimensions via \code{\link[=Expand]{Expand()}}.
}
}
\description{
A matched set of functions for saving and loading \link[MSEtool:hist-class]{MSEtool::hist} objects to
disk. \code{SaveHist()} compresses and writes a \code{.hist} file locally. \code{ListHist()}
lists available \code{.hist} files in a directory. \code{LoadHist()} loads and expands
a saved \code{.hist} file back to full array dimensions.
}
\details{
Output files are ignored by git and must be generated locally.
}
\seealso{
\code{\link[=Compress]{Compress()}}, \code{\link[=Expand]{Expand()}}, \code{\link[MSEtool:Save]{MSEtool::Save()}}
}
