% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find-early-late-fleets.R
\name{FindEarlyLateFleets}
\alias{FindEarlyLateFleets}
\title{Identify Early/Late Fleet Pairs by Fuzzy Name Matching}
\usage{
FindEarlyLateFleets(fleets, max_dist = 3)
}
\arguments{
\item{fleets}{Character vector of fleet names. Names are expected to follow
the convention \verb{F\{n\}_\{fleet_name\}_\{early|late\}}, e.g.
\code{"F1_JPN_WCNPO_OSDWLL_late_Area1"}. The numeric \verb{F\{n\}_} prefix and the
\code{early}/\code{late} token are stripped before matching. Fleets without an
\code{early} or \code{late} tag are ignored.}

\item{max_dist}{Non-negative integer. Maximum edit distance (via
\code{\link[utils:adist]{utils::adist()}}) allowed between two base fleet names for them to be
considered a match. Increase to tolerate greater name variation; decrease
to require closer matches. Default: \code{3}.}
}
\value{
A named list. Each element corresponds to a matched early/late pair,
where:
\itemize{
\item The \strong{name} is the canonical base fleet name (the shortest name in the
group, with the \verb{F\{n\}_} prefix and \code{early}/\code{late} token removed).
\item The \strong{value} is a character vector of the original fleet names from
\code{fleets} that belong to the pair, sorted alphabetically.
}

Returns an empty list if no early/late pairs are found.
}
\description{
Given a character vector of fleet names, finds fleets that represent the
same fishery but differ only by an \code{early} or \code{late} temporal tag, using
fuzzy string matching to accommodate minor naming inconsistencies (e.g.
\code{OSDWLL} vs \code{OSDWCOLL}).
}
\seealso{
\code{\link[utils:adist]{utils::adist()}} for the underlying edit-distance computation,
\code{\link[MSEtool:name-accessors]{MSEtool::FleetNames()}} to extract fleet names from an OM object.
}
