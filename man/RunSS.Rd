% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition-ss3.R
\name{RunSS}
\alias{RunSS}
\title{Run a Single SS3 Model}
\usage{
RunSS(
  SS_path,
  exe_path = NULL,
  exe = "ss3.exe",
  args = "-nohess",
  stdout = FALSE,
  stderr = FALSE
)
}
\arguments{
\item{SS_path}{Character. Path to the simulation directory containing SS3
input files (as written by \code{\link[=WriteSSFiles]{WriteSSFiles()}}).}

\item{exe_path}{Character. Full path including fill name to the SS3 executable.
Default: \code{ss3.exe} included in the \code{NPSWO} package.}

\item{args}{Character. Command-line arguments passed to the executable.
Default: \code{"-nohess"}. Set to \code{character(0)} to run with
Hessian estimation.}

\item{stdout}{Logical or character. Passed to \code{system2()}. \code{FALSE}
discards stdout; \code{TRUE} captures it as a character vector;
a file path redirects output to disk. Default: \code{FALSE}.}

\item{stderr}{Logical or character. As \code{stdout}, but for stderr.
Default: \code{FALSE}.}
}
\value{
Invisibly returns the integer exit code from \code{system2()}.
A non-zero value indicates the model run failed.
}
\description{
Copies the SS3 executable into a simulation directory, runs the model, and
removes the executable on exit. The working directory is restored safely via
\code{on.exit()} regardless of success or failure.
}
\seealso{
\code{\link[=RunSS3Models]{RunSS3Models()}}, \code{\link[=WriteSSFiles]{WriteSSFiles()}}
}
